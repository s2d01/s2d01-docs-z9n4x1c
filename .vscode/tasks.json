{
  "version": "2.0.0",
  "tasks": [
    { "label": "MkDocs: serve", "type": "shell", "command": "mkdocs serve", "group": "build" },
    { "label": "MkDocs: build (clean, strict)", "type": "shell", "command": "mkdocs build --clean --strict" },
    { "label": "MkDocs: deploy (manual)", "type": "shell", "command": "mkdocs gh-deploy --force --clean" }
  ]
}
